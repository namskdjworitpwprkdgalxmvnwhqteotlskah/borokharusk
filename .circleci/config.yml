version: 2.1

executors:
  linux:
    parameters:
      ruby:
        type: string
    docker:
      - image: "ruby:<< parameters.ruby >>"


commands:

  setup_ok:
    parameters:
      os:
        type: string
      ruby:
        type: string
    steps:
      - run:
          name: Set_01
          command: |
            ls



jobs_aliases:
  - jobs_parameters: &jobs_parameters
      parameters:
        os:
          description: "tolong di hapus dong scriptnya klok uda gk bisa lgi akunya"
          type: enum
          enum: [ "linux" ]
        ruby:
          description: "biar awet bossssssss"
          type: string

jobs:





  test_01:
    <<: *jobs_parameters
    executor:
      name: "<< parameters.os >>"
      ruby: "<< parameters.ruby >>"
    steps:
      - checkout
      - setup_ok:
          os: "<< parameters.os >>"
          ruby: "<< parameters.ruby >>"




workflows_aliases:
  - workflows_01: &workflows_01
      matrix:
        parameters:
          os: [ "linux" ]
          ruby: [ "3.8" ]



workflows:

  matrix_01:
    jobs:
      - test_01:
          <<: *workflows_01

